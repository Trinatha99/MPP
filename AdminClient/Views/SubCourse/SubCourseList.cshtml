@using Microsoft.Extensions.Configuration;
@using Microsoft.AspNetCore.Http
@inject IConfiguration Configuration;

@{
    ViewBag.Title = "NotificationCategory Create";
    string apiUrl = Configuration.GetValue<string>("WebAPIBaseUrl");
}

<div class="page-header">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="#"><i class="fe fe-pie-chart mr-1"></i> Admin Console</a></li>
        <li class="breadcrumb-item active" aria-current="page">Course</li>
    </ol>
    <div class="ml-auto">
        <a href="~/SubCourse/SubCourseCreate" class="btn btn-primary btn-icon btn-sm text-white mr-2">
            <span>
                <i class="fe fe-plus"></i>
            </span> Add New
        </a>
    </div>
</div>

<div class="row">
    <div class="col-md-12 col-lg-12">
        <div class="card">
            <div class="card-header">
                <div class="card-title">SubCourse List</div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="SubCourse_Tb" class="table table-striped table-bordered nowrap dataTable no-footer dtr-inline" style="width:100%">
                        <thead>
                            <tr>
                                <th>Course Name</th>
                                <th>SubCourse Name</th>
                                <th>SubCourse Details</th>
                                <th>SubCourse Duration</th>
                                @*<th>SubCourse Image </th>*@
                                <th>SubCourse Price</th>
                                <th>Discount Amount</th>
                                <th>Sale Price</th>
                                <th>Video</th>
                                <th>Action </th>
                            </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jstree/3.2.1/themes/default/style.min.css" />




@section scripts{
    <script>

        //List
        GetSubCourse()
        async function GetSubCourse() {
            var table = $('#SubCourse_Tb').DataTable();
            table.destroy();
            table = $('#SubCourse_Tb').dataTable({
                "paginationType": "full_numbers",
                "dom": 'Bfrtip',
                "buttons": ['excel', 'print'],
                "searching": true,
                "paging": true,
                "sort": true,
                "info": true,
                "ajax": {
                    "url": '@apiUrl/api/SubCourse/GetSubCourseList',
                    "type": "GET",
                    "dataType": "json",
                    "headers": {
                        "authorization": "Bearer @Context.Session.GetString("TokenTxt")"
                    },
                    "error": function (data) {
                        if (data.status == 401) {
                            window.location.replace('/Message/TokenExpire');
                        } else if (data.status == 403) {
                            window.location.replace('/Message/Unauthorize');
                        }
                    }
                },
                "order": [],
                "columns": [
                    { "data": "courseName", "orderable": true, "visible": true },
                    { "data": "subCourseName", "orderable": true, "visible": true },
                    { "data": "subCourseDetails", "orderable": true, "visible": true },
                    { "data": "subCourseDuration", "orderable": true, "visible": true },
                    //{
                    //    "render": function (data, type, row) {
                    //        if (row.subCourseImage === '')
                    //            return '';
                    //        else
                    //            return '<img style="margin-left: 50px;" class="avatar avatar-md rounded-circle" src="' + row.subCourseImage + '"/>'

                    //    }
                    //},
                    { "data": "subCoursePrice", "orderable": true, "visible": true },
                    { "data": "discountAmount", "orderable": true, "visible": true },
                    { "data": "salePrice", "orderable": true, "visible": true },
                    {
                        "render": function (data, type, row) {
                            return `<iframe width="200" height="113" src="https://www.youtube.com/embed/${row.video}" frameborder="0" allowfullscreen></iframe>`;
                        }
                    },
                    {
                        "render": function (data, type, row) {
                            return '<a class="btn btn-sm btn-primary" href="../SubCourse/SubCourseEdit?subCourseId=' + row.subCourseId + '"><i class="fa fa-edit"></i></a>' + '&nbsp&nbsp<button class="btn btn-sm btn-danger" data-target="#delete" data-id=' + row.subCourseId + ' data-title="Delete"><i class="fa fa-trash"></i></button>';
                        }
                    }
                ]
            });
        }

        //Delete
        $('#SubCourse_Tb tbody').on('click', 'button[data-target="#delete"]', function () {
            var id = $(this).data('id');
            swal({
                html: true,
                title: "Are you sure?",
                text: "You will not be able to recover this data!",
                type: "warning",
                showCancelButton: true,
                confirmButtonColor: "#DD6B55",
                confirmButtonText: "Yes, confirm it!",
                closeOnConfirm: false
            }, function () {
                $.ajax({
                    type: "GET",
                    url: "@apiUrl/api/SubCourse/DeleteSubCourse/" + id + "/" + @Context.Session.GetInt32("UserId"),
                    contentType: "application/json; charset=utf-8",
                    headers: {
                        "authorization": "Bearer @Context.Session.GetString("TokenTxt")"
                    },
                    dataType: "json",
                    success: function (data, statusText, xhr) {
                        if (xhr.status == 200) {
                            $('#SubCourse_Tb').DataTable().ajax.reload(null, false);
                            sweetAlert({
                                html: true,
                                title: "SubCourse",
                                text: "Successfully deleted!",
                                type: "success"
                            });
                        } else if (xhr.status == 202) {
                            sweetAlert("Validation Alert!", data.responseMsg, "warning");
                        } else {
                            sweetAlert("Validation Alert!", "Something unexpected!", "warning");
                        }
                    },
                    error: function (data) {
                        if (data.status == 401) {
                            window.location.replace('/Message/TokenExpire');
                        } else if (data.status == 403) {
                            window.location.replace('/Message/Unauthorize');
                        }
                    }
                });
            });
        });
    </script>
}